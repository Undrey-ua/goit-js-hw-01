const total = 100;
const ordered = 50;
let order;

if (ordered <= total) {
 const order = "Заказ оформлен, с вами свяжется менеджер";
 console.log(order);
} else {
 const order = "На складе недостаточно твоаров!";
    console.log(order)
}
